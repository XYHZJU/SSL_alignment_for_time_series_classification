from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Measure alignment between the self-supervised pre-training task and downstream tasks on multi variable time series data.',
    author='Yuhan Xie',
    license='BSD-3',
)
